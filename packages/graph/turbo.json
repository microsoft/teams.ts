{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "!dist/cache/**"],
      "cache": false,
      "dependsOn": ["@microsoft/teams.common#build"]
    }
  }
}
