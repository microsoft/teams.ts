# Sample environment variables for the Python AI template
# Rename this file to .env and fill in the values. Do NOT commit secrets to version control.
# These variables are consumed by the app (via python-dotenv or the process environment)

# OpenAI API key (required)
# Get your key from https://platform.openai.com/ and keep it secret.
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI model to use (default: gpt-4o)
# Change to another model name if needed, e.g. gpt-4, gpt-4o-mini
OPENAI_MODEL=gpt-4o

# Port the app will listen on (default: 3978)
# You can override when running: PORT=5000 python -m your_app
PORT=3978

# End of file