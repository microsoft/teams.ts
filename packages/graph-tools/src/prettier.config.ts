import { Config } from 'prettier';

/**
 * @see https://prettier.io/docs/en/configuration.html
 */
const config: Config = {
  trailingComma: 'es5',
  semi: true,
  singleQuote: true,
  printWidth: 100,
};

export default config;
