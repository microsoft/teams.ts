/**
 * @deprecated `@microsoft/teams.openai` is deprecated. Use the `openai` SDK
 * directly (the `AzureOpenAI` client with `chat.completions.runTools`).
 * See `examples/ai-mcp` for the new pattern.
 */
export * from './chat';
export * from './audio';
