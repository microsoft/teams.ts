{
  "name": "@microsoft/teams.integration-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Integration tests for Teams TypeScript SDK — runs against live Teams service endpoints",
  "scripts": {
    "test:integration": "jest --config jest.config.ts",
    "test:botid-prod": "dotenv -e .env.botid-prod -- jest --config jest.config.ts",
    "test:agentic-identity-prod": "dotenv -e .env.agentic-identity-prod -- jest --config jest.config.ts"
  },
  "devDependencies": {
    "@azure/msal-node": "^5.6.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^22.0.2",
    "dotenv-cli": "^7.4.0",
    "jest": "^30.5.1",
    "ts-jest": "^29.4.12",
    "typescript": "~5.8.0"
  },
  "dependencies": {
    "@microsoft/teams.api": "*",
    "@microsoft/teams.common": "*"
  }
}
