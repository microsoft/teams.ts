{
  "name": "@examples/langchain-emoji-agent",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "main": "dist/index",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "npx rimraf ./dist",
    "lint": "npx eslint",
    "lint:fix": "npx eslint --fix",
    "build": "npx tsc",
    "start": "node -r dotenv/config .",
    "dev": "tsx watch -r dotenv/config src/index.ts",
    "cli": "tsx -r dotenv/config src/cli.ts"
  },
  "dependencies": {
    "@langchain/core": "^0.3.0",
    "@langchain/openai": "^0.3.0",
    "express": "^5.0.0"
  },
  "devDependencies": {
    "@microsoft/teams.config": "*",
    "@types/express": "^5.0.0",
    "@types/node": "^22.5.4",
    "dotenv": "^16.4.5",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.6",
    "typescript": "^5.4.5"
  }
}
