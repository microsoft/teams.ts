/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable default server since we're using a custom server
  // This is implicit when using custom server, but good to be explicit
};

module.exports = nextConfig;
