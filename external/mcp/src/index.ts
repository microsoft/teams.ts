/**
 * @deprecated `@microsoft/teams.mcp` is deprecated. Use
 * `@modelcontextprotocol/sdk` directly. See `examples/ai-mcp` and
 * `examples/mcp-server` for the new pattern.
 */
export * from './plugin';
