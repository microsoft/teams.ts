/**
 * @deprecated `@microsoft/teams.mcpclient` is deprecated. Use
 * `@modelcontextprotocol/sdk` directly with the `openai` SDK's
 * `chat.completions.runTools`. See `examples/ai-mcp` for the new pattern.
 */
export * from './mcp-client-plugin';
export * from './mcp-client-types';
